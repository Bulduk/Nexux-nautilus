kind = "web"
previewPath = "/"
title = "Dataclaw / Nexus OS"
version = "1.0.0"
id = "artifacts/dataclaw"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20395

[services.development]
run = "pnpm --filter @workspace/dataclaw run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/dataclaw", "run", "build" ]
publicDir = "artifacts/dataclaw/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20395"
BASE_PATH = "/"
